import React from 'react';

const Counter = (props) => {

    return (
        <div className='res-counter'>
            <h2 >wynik: {props.counter} </h2>
        </div>
    
    )
}

export default Counter;